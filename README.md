# Concrete Crack Detection

## Run the App
From the project root, run:

```bash
streamlit run src/predict.py
```

## Troubleshooting

If you see `zsh: command not found: streamlit`, activate your virtual environment (e.g., `source .venv/bin/activate`) and make sure Streamlit is installed with `pip install streamlit` before running the command above.
